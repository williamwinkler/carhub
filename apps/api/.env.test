# Test environment configuration
# Separate test database to avoid conflicts with development data

NODE_ENV=test
PORT=3001

# Test JWT secrets (can be simple for testing)
JWT_ACCESS_SECRET=test_access_secret_key_123
JWT_REFRESH_SECRET=test_refresh_secret_key_456

# CORS
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Test Database Configuration (separate database to avoid conflicts)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DATABASE=demo_test_db
POSTGRES_USERNAME=admin
POSTGRES_PASSWORD=admin